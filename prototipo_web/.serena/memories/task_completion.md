# Task Completion
- Run smallest relevant interactive check in local Vite app.
- Required static validation: `npm run lint` and `npm run build` (includes `tsc -b`).
- Verify `/#/onboarding` opens, target behavior works, console has no fresh errors.
- Check responsive state when UI changes.
- No hardcoded color/font/dimension outside established tokens/patterns.