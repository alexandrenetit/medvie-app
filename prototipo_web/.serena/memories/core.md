# Prototype Web
- Isolated SPA in `prototipo_web/`; 100% mock data, no backend.
- Source under `src/`; app pages use `AppShell`; login/onboarding stay outside shell.
- HashRouter; root without hash resolves to login.
- Never enable React StrictMode: existing AnimatePresence exit-cycle constraint.
- Stack details: `mem:tech_stack`.
- Code/design conventions: `mem:conventions`.
- Completion checks: `mem:task_completion`.
- Working commands: `mem:suggested_commands`.